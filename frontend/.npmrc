# react-day-picker@8 declares a peer of date-fns ^2||^3 while this project is on
# date-fns@4. npm 7+ treats that as a hard failure, and with no committed
# lockfile every Netlify build re-resolves from scratch and would hit it.
legacy-peer-deps=true
