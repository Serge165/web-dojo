{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "../backend/dist/webdojo-backend",
          "sidecar": true
        }
      ]
    },
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    {
      "comment": "The .webdj format is a user-chosen document file, so the path is whatever the native picker returns — it may be on an external drive or any mount, not just $HOME. Scope is therefore unrestricted, but only for single-file read/write; directory listing, remove, rename and mkdir are deliberately not granted.",
      "identifier": "fs:allow-read-file",
      "allow": [
        {
          "path": "**"
        }
      ]
    },
    {
      "identifier": "fs:allow-write-file",
      "allow": [
        {
          "path": "**"
        }
      ]
    }
  ]
}
